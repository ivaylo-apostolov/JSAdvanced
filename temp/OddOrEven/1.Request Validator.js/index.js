function solve(input) {
    let keys = Object.keys(input);

    function checkAllProps(keys){
         let haveAllElements = keys.find(x => x === 'method'
        || x === 'uri'
        || x === 'version'
        || x === 'message')
}

solve(
    {
        method: 'GET',
        uri: 'svn.public.catalog',
        version: 'HTTP/1.1',
        message: ''
    }
)